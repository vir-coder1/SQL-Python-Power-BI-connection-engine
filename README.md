# SQL-Python-Power-BI-connection-engine
Replaced a manually-maintained multi-tab Excel model with a SQL + Python + Power BI pipeline — dashboard refresh dropped from a multi-minute manual rebuild to a single click reading live from the database.
